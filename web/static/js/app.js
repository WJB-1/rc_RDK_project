// 页面唯一入口：快照驱动渲染，控制命令不直接触碰导航对象。
(function(){function update(data){const n=data.navigation||{};const set=(id,v)=>{const e=document.getElementById(id);if(e)e.textContent=v==null?'--':v};set('nav-state',n.state);set('current-action',n.current_action&&n.current_action.action_id);set('current-request',n.current_request&&n.current_request.request_id);set('map-version',n.runtime_map&&n.runtime_map.version);const p=n.progress||{};set('task-progress',p.completed==null?'--':(p.completed+'/'+(p.total||'?')));set('runner-state',data.running?'运行中':(data.paused?'已暂停':'未启动'));set('clock',data.now==null?'--':data.now.toFixed?data.now.toFixed(2):data.now);const d=document.getElementById('diagnostics');if(d)d.textContent=(n.diagnostics||[]).join('\n')||'--';renderMap(data);renderTimeline(data.events)}function command(name){const body=name==='RESET'?{seed:0}:{};return fetch('/api/sim/'+name.toLowerCase(),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}).then(r=>r.json()).then(update)}bindControls(command);document.getElementById('truth-toggle').addEventListener('change',()=>document.body.classList.toggle('show-truth'));connectSnapshot(update);fetch('/api/snapshot').then(r=>r.json()).then(update).catch(()=>{})})();
