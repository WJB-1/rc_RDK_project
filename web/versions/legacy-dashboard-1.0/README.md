# Legacy Dashboard 1.0

This bundle preserves the original production and development dashboard pages.
They are historical artifacts, not the default Navigation 2.0 entry point.

Both templates contain their original JavaScript and CSS inline. The former
development page depends on a historical `dashboard.js` that is not present,
so it is retained for reference only.
