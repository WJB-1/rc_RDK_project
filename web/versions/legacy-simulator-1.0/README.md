# Legacy Simulator 1.0

This is the preserved simulator visual-debugging bundle. `WebPushServer`
serves `templates/simulator.html` at `/simulator` and exposes its scripts at
`/legacy-static`.

It is intentionally kept outside `waste`: its Canvas map renderer and
interaction layer are the planned starting point for a future Navigation 2.0
simulation debugger.
